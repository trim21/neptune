[project]
name = "neptune-e2e"
version = "0.0.0"
requires-python = ">=3.12"
dependencies = [
    "bencode2>=0.1",
    "httpx>=0.28",
    "pytest>=8",
    "pytest-timeout>=2",
]

[tool.pytest.ini_options]
markers = [
    "e2e: end-to-end integration tests",
]
